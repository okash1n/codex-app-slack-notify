# Contributing

Issues and pull requests are welcome.

## Development

- Run tests:

```bash
python3 -m unittest
```

## Notes

- This project reads macOS notification UI via AppleScript (Accessibility + Automation permissions).
- Please avoid committing personal paths or secrets (e.g. Slack webhook URLs).
